'use strict';

/**
 * festive-leads-gift router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::festive-leads-gift.festive-leads-gift');
