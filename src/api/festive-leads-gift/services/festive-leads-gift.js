'use strict';

/**
 * festive-leads-gift service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::festive-leads-gift.festive-leads-gift');
