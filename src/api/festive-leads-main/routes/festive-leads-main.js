'use strict';

/**
 * festive-leads-main router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::festive-leads-main.festive-leads-main');
