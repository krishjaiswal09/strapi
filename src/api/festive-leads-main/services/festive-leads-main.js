'use strict';

/**
 * festive-leads-main service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::festive-leads-main.festive-leads-main');
