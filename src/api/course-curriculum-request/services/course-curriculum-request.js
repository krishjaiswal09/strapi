'use strict';

/**
 * course-curriculum-request service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::course-curriculum-request.course-curriculum-request');
