'use strict';

/**
 * course-curriculum-request router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::course-curriculum-request.course-curriculum-request');
